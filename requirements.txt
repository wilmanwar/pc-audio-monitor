sounddevice>=0.4.5
numpy>=1.21.0
requests>=2.28.0
python-dotenv>=1.0.0
pyinstaller>=6.0.0

# Optional — enables pretrained ONNX genre classifier (much more accurate).
# After installing, download model files per the instructions in music_classifier.py.
# The app works without these — falls back to heuristic classification.
librosa>=0.10.0
onnxruntime>=1.17.0